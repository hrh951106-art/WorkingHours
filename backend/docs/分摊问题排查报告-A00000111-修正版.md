# 分摊问题排查报告（修正版）- 员工 A00000111

## 问题描述
员工 A00000111 在 2026年3月15日使用分摊规则 A0100001 未产生分摊结果

## 修正后的分摊逻辑理解

### 新的分摊规则逻辑
1. **不需要分摊目标配置** - 已废弃
2. **分摊范围（allocationScopeId）** - 控制按什么账户层级范围分摊
   - 例如：车间层级（levelId: 29）= 取车间下所有产线
3. **分配归属（allocationHierarchyLevels）** - 分摊到哪里
   - 例如：线体层级（levelId: 30）= 按产线分摊

### 分摊流程
```
1. 获取当天所有班次所开的产线（LineShift表）
   - scheduleDate = 2026-03-15
   - status = 'ACTIVE'
   - participateInAllocation = true

2. 按班次过滤产线
   - 根据工时记录的shiftId过滤

3. 如果该班次没有产线配置 → 跳过分摊
```

## 排查结果

### ✅ 员工信息
- 员工编号：A0000011
- 姓名：测试员工1
- 所属车间：W1总装车间（ID: 6）
- 状态：ACTIVE

### ✅ 工时记录
- 计算日期：2026-03-15
- 班次ID：1（早班）
- 出勤代码：I04（车间工时）
- 实际工时：8.0 小时
- 账户：富阳工厂/W1总装车间/////间接设备

### ✅ 分摊配置
- 配置ID：20
- 配置代码：A0100001
- 配置名称：车间辅助工时分摊
- 状态：ACTIVE

### ✅ 分摊规则
- 规则ID：30
- 规则名称：分摊规则1
- 规则类型：PROPORTIONAL（按比例分摊）
- 分摊依据：ACTUAL_HOURS（按实际工时）
- 分摊范围：车间层级（levelId: 29）
- 分配归属：线体层级（levelId: 30，allocationHierarchyLevels: "[37]"）

### ❌ 根本原因：班次1在2026-03-15没有产线配置

**查询结果**：
```bash
# 查询班次1在2026-03-15的产线配置
SELECT ls.lineId, p.code as lineCode, p.name as lineName
FROM LineShift ls
LEFT JOIN ProductionLine p ON ls.lineId = p.id
WHERE ls.scheduleDate = '2026-03-15'
  AND ls.shiftId = 1;

# 结果：0条记录
```

**验证查询**：
```bash
# 查询2026-03-15所有班次的产线配置
SELECT ls.shiftId, COUNT(*) as line_count
FROM LineShift ls
WHERE ls.scheduleDate = '2026-03-15'
  AND ls.deletedAt IS NULL
GROUP BY ls.shiftId;

# 结果：空（说明当天没有任何产线配置）
```

## 问题分析

### 代码逻辑（allocation.service.ts 第1753-1759行）

```javascript
// 获取该班次当天开的产线列表（直接使用工时记录中的班次ID）
const shiftLines = activeLines.filter((line) => line.shiftId === aggregatedResult.shiftId);

if (shiftLines.length === 0) {
  console.log(`班次 ${aggregatedResult.shiftName} (ID:${aggregatedResult.shiftId}) 在 ${dateKey} 没有开线，跳过 (员工:${aggregatedResult.employeeNo}, 工时:${aggregatedResult.actualHours})`);
  continue;
}
```

### 执行流程

1. **查询工时记录** ✅
   - 找到员工 A0000011 在 2026-03-15 的工时记录
   - 班次ID：1（早班）

2. **获取产线配置** ❌
   - getActiveLinesForDate('2026-03-15') 返回空数组
   - 原因：LineShift 表中没有 2026-03-15 的记录

3. **过滤班次产线** ❌
   - shiftLines = activeLines.filter(line => line.shiftId === 1)
   - 结果：shiftLines.length = 0

4. **跳过分摊** ❌
   - 打印日志："班次 早班 (ID:1) 在 2026-03-15 没有开线，跳过 (员工:A0000011, 工时:8.0)"
   - 不产生任何分摊结果

## 解决方案

### 方案1：配置产线（推荐）

在产线管理模块为班次1添加2026-03-15的产线配置：

1. **进入产线配置页面**
   - 选择日期：2026-03-15
   - 选择班次：早班（ID: 1）

2. **添加产线**
   ```
   产线1：L1产线
   - 计划产量：根据实际情况填写
   - 参与分摊：✅
   - 状态：ACTIVE
   ```

3. **保存配置**

4. **重新执行分摊计算**
   - 配置：A0100001
   - 日期范围：2026-03-15

### 方案2：使用有产线配置的日期

如果2026-03-15不需要分摊，可以：

1. 选择有产线配置的日期执行分摊
2. 或者检查产线配置数据是否缺失

### 方案3：检查产线配置数据

查询近期哪些日期有产线配置：

```sql
SELECT
  date(ls.scheduleDate) as calcDate,
  ls.shiftId,
  ls.shiftName,
  COUNT(*) as lineCount
FROM LineShift ls
WHERE ls.deletedAt IS NULL
  AND ls.scheduleDate >= '2026-03-01'
GROUP BY date(ls.scheduleDate), ls.shiftId, ls.shiftName
ORDER BY calcDate DESC, ls.shiftId
LIMIT 20;
```

## 验证步骤

### 1. 添加产线配置后验证

```bash
# 查询班次1的产线配置
SELECT ls.lineId, p.code as lineCode, p.name as lineName, ls.participateInAllocation
FROM LineShift ls
LEFT JOIN ProductionLine p ON ls.lineId = p.id
WHERE ls.scheduleDate = '2026-03-15'
  AND ls.shiftId = 1
  AND ls.deletedAt IS NULL;

# 预期：至少返回1条记录
```

### 2. 重新执行分摊

```bash
POST /api/allocation/calculate
{
  "configId": 20,
  "startDate": "2026-03-15",
  "endDate": "2026-03-15"
}
```

### 3. 查看日志

预期看到：
```
[分摊计算] 处理汇总记录: 员工=A0000011, 出勤代码ID=6, 班次ID=1, 班次名称=早班, 实际工时=8.0
班次 早班 (ID:1) 在 2026-03-15 的产线: L1产线, L2产线...
[分摊计算] 产线 L1产线 班次 早班: 直接工时=xxx, 系数=xxx, 分摊工时=xxx
```

### 4. 验证分摊结果

```sql
SELECT
  sourceEmployeeNo,
  attendanceCode,
  sourceHours,
  targetName,
  allocatedHours
FROM AllocationResult
WHERE configId = 20
  AND recordDate = '2026-03-15'
  AND sourceEmployeeNo = 'A0000011';

# 预期：返回该员工的分摊记录
```

## 数据完整性检查

### 检查清单
- [x] 员工存在且状态为 ACTIVE
- [x] 员工在指定日期有工时记录
- [x] 工时记录的出勤代码匹配（I04）
- [x] 员工账户在分摊源筛选范围内
- [x] 分摊配置状态为 ACTIVE
- [x] 分摊规则存在且状态为 ACTIVE
- [x] 分摊范围和分配归属配置正确
- [❌] **班次在指定日期有产线配置** ← 问题所在

## 总结

**问题**：员工 A00000111 在 2026年3月15日使用分摊规则 A0100001 未产生分摊结果

**根本原因**：班次1（早班）在2026-03-15没有配置任何产线

**影响**：
- 所有在班次1工作且在2026-03-15参与分摊的员工都无法产生分摊结果
- 不是该员工特有的问题，而是该班次该日期的系统配置问题

**解决方案**：为班次1添加2026-03-15的产线配置

**优先级**：🔴 高（导致整个班次的员工都无法分摊）

**相关模块**：
- 产线配置管理（LineShift）
- 分摊计算
- 班次管理

---

**报告生成时间**：2026-03-31
**报告人**：AI Assistant
**相关问题**：产线配置缺失导致分摊失败
